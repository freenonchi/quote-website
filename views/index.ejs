<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quotes</title>
  <link rel="stylesheet" href="/public/styles.css">
</head>
<body>
  <div class="container">
    <h1>Quotes</h1>
    <% if (user) { %>
      <p>Welcome, <%= user.username %>!</p>
      <a href="/new-quote">New Quote</a> | <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
    <% } %>
    <ul>
      <% quotes.forEach(quote => { %>
        <li class="quote">
          <blockquote><%= quote.text %></blockquote>
          <p class="author">by <%= quote.author %></p>
          <p class="tags">Tags: <span><%= quote.tags || 'N/A' %></span></p>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>